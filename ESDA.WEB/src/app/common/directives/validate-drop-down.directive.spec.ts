import { ValidateDropDownDirective } from "./validate-drop-down.directive";

describe("ValidateDropDownDirective", () => {
  it("should create an instance", () => {
    const directive = new ValidateDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
